# marketingAI
it is a marketin full stack website which provide a fully automated marketing strategy with marketing tamplate
# initiate backend

npm init -y
npm install express cors dotenv axios cheerio groq-sdk
npm @huggingface/transformers

# create your hugging face and groq api key's

# initiate frontend

npm create vite@latest 
cd frontend
npm install axios
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p

# done👍
